import discord
from discord.ext import commands

# 1. Reemplaza 'TU_TOKEN_DEL_BOT' con el token de tu bot.
# 2. Reemplaza 'ID_DEL_ROL' con el ID numérico del rol. Puedes obtenerlo activando el modo de desarrollador en Discord
# y haciendo clic derecho en el rol.

TOKEN = 'MTQwNzk0NDc0MDA2MTgzOTQ5MA.G7fcFK.L2LnPy1f_FJEsSiCLyxRjFoi4UIkguOuFtaRHo'
ROL_ID = 1407948816321351732

intents = discord.Intents.default()
intents.message_content = True  # Necesario para leer el contenido de los mensajes

bot = commands.Bot(command_prefix='!', intents=intents)

@bot.event
async def on_ready():
    print(f'¡Bot conectado como {bot.user}!')

@bot.command()
async def llamen(ctx):
    """
    Este comando hace una mención a un rol específico.
    Se activa escribiendo '!llamen' en el chat.
    """
    role = ctx.guild.get_role(ROL_ID)
    if role:
        # Aquí se crea el mensaje, mencionando al rol.
        await ctx.send(f'¡Hola, {role.mention}! Se les necesita en el servidor.')
    else:
        await ctx.send(f'No se encontró el rol con el ID {ROL_ID}.')

# Inicia el bot con tu token
bot.run(MTQwNzk0NDc0MDA2MTgzOTQ5MA.G7fcFK.L2LnPy1f_FJEsSiCLyxRjFoi4UIkguOuFtaRHo)

